# Copy to .env and enter your own key locally. Never commit .env.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b
# Embeddings run locally. The model is downloaded on first use.
# This learning version fixes embeddings and chunk settings in nexus_rag/core.py.
# Changing them requires a new collection name and re-indexing.
